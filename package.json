{
  "name": "kakoucalc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/fetch-data.mjs && astro build",
    "build:offline": "astro build",
    "preview": "astro preview",
    "refresh-data": "node scripts/fetch-data.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.0"
  }
}
